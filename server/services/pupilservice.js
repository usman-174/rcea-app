/// for pupipl curd operations and complex task
/// for pupipl curd operations and complex task


const checkEmail = (email) => {
  // var query = "select * from dw.stage.user where email = '" + email + "'"
  // let id = ''
  // executeQuery(query).then((re) => {
  //   console.log('this is     ' + re.ID)
  //   id = re.ID
  //   return id
  // })
}

module.exports = {
  checkEmail,
}
